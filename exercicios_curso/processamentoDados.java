public class processamentoDados {
    public static void main(String[] args) {  
        //ex (base + BASE) / 2 * altura(h)
        double b = 6.0;
        double B = 8.0;
        double h = 5.0;
        double trapezio = (b + B) / 2 * h;

        System.out.println("Area do trapezio eh: " + trapezio);
    }
}
